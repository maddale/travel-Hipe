I"•(function() {
  $(function() {
    return $('.card-header').click(function() {
      var currentId, id;
      id = this.id;
      currentId = this.getAttribute('data-current-user');
      if (this.getAttribute('data-cheked')) {
        $.ajax({
          url: "/users/" + currentId + "/messages/" + id,
          type: 'GET',
          dataType: 'script'
        });
        this.setAttribute('data-cheked', '');
      } else {
        $('.talk-with-' + id).fadeOut(300);
        this.setAttribute('data-cheked', 'ready');
      }
      return $('.delete-message').click(function() {
        return console.log(this.id);
      });
    });
  });

}).call(this);
:ET