<h1>Edit profile</h1>

<div class="row">
	<%= form_for(@user) do |f| %>
		<%= render 'forms_errors' %>
		<%= render 'user_form', f: f %>
	

	<%= f.submit 'Edit profile', class: "btn btn-large btn-primary" %>
	<% end %>



</div>