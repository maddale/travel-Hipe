
	
	<li>
		<span class="content">
		<%= micropost.id %><br>
		<%= micropost.content %>
		</span>
		<span class="timestamp"><small>Posted <%= time_ago_in_words(micropost.created_at) %> ago.</small></span><br>
		
<% if current_user.id == micropost.user_id %>
		<small><span><%= link_to "Delete", micropost_path(micropost), method: :delete, data: { confirm: "Sure?" } %></span></small>
<% end %>
	</li>
